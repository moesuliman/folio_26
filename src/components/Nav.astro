---
const pathname = Astro.url.pathname;

const links = [
	{
		href: '/',
		label: 'Work',
		match: (path: string) => path === '/' || path === '/work' || path.startsWith('/work/'),
	},
	{
		href: '/blog',
		label: 'Play',
		match: (path: string) => path === '/blog' || path.startsWith('/blog/'),
	},
];
---

<header class="site-header">
	<nav class="site-nav" aria-label="Primary">
		<ul class="site-nav__links">
			{
				links.map((link) => (
					<li>
						<a href={link.href} aria-current={link.match(pathname) ? 'page' : undefined}>
							{link.label}
						</a>
					</li>
				))
			}
		</ul>
	</nav>
</header>
